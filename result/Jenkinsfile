pipeline {
  agent {
    label 'metr0'
  }

  stages {
    stage('build') {
      steps {
        sh 'npm install'
      }
    }

    stage('test') {
      steps {
        sh 'npm test'
      }
    }
  }
}
