pipeline {
  agent {
    docker {
      image 'node:alpine'
    }
  }

  stages {
    stage('build') {
      steps {
        dir 'result'
        sh 'npm install'
      }
    }

    stage('test') {
      steps {
        dir 'result'
        sh 'npm test'
      }
    }
  }
}
